<Window x:Class="Project_QLTS_DNC.Views.LichSuBaoTriWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
        Title="Lịch sử bảo trì tài sản" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Màu Background chính -->
        <Color x:Key="Primary">#0071f5</Color>
        <Color x:Key="PrimaryLight">#3387c8</Color>
        <Color x:Key="PrimaryDark">#004f8a</Color>

        <!-- Màu Background Button Chính -->
        <SolidColorBrush x:Key="PrimaryHueMidBrush" Color="{StaticResource Primary}"/>
        <SolidColorBrush x:Key="PrimaryHueDarkBrush" Color="{StaticResource PrimaryDark}"/>
        <SolidColorBrush x:Key="PrimaryHueLightBrush" Color="{StaticResource PrimaryLight}"/>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{StaticResource PrimaryHueMidBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHueDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" Margin="15" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tiêu đề -->
            <Grid Grid.Row="0" Margin="20,20,20,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="History" 
                                           Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                           Margin="0,0,10,0"
                                           Width="28"
                                           Height="28"
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="LỊCH SỬ BẢO TRÌ TÀI SẢN" 
                             FontSize="24" 
                             FontWeight="Bold" 
                             Foreground="{DynamicResource PrimaryHueMidBrush}"
                             VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Thanh tìm kiếm và lọc -->
            <Grid Grid.Row="1" Margin="20,20,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <materialDesign:Card Grid.Column="0" Padding="0" Margin="0" UniformCornerRadius="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="Search" 
                        Grid.Column="0" 
                        VerticalAlignment="Center" 
                        Margin="12,0" 
                        Foreground="{StaticResource PrimaryHueMidBrush}"/>
                        <TextBox Grid.Column="1" 
                    x:Name="txtTimKiem" 
                    materialDesign:HintAssist.Hint="Tìm kiếm theo tên tài sản, số seri..." 
                    BorderThickness="0" 
                    VerticalAlignment="Center" 
                    Padding="5,12"
                    Background="Transparent"
                    KeyDown="txtTimKiem_KeyDown"
                    materialDesign:TextFieldAssist.UnderlineBrush="{StaticResource PrimaryHueMidBrush}"/>
                        <Button Grid.Column="2" 
                    Style="{StaticResource MaterialDesignIconButton}" 
                    x:Name="btnTimKiem" 
                    Margin="0,0,8,0"
                    Click="btnTimKiem_Click"
                    ToolTip="Tìm kiếm">
                            <materialDesign:PackIcon Kind="ArrowRight" Foreground="{StaticResource PrimaryHueMidBrush}"/>
                        </Button>
                    </Grid>
                </materialDesign:Card>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ComboBox x:Name="cboLoaiHoatDong" Width="150" Height="42" Margin="15,0" 
               HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
               SelectionChanged="cboLoaiHoatDong_SelectionChanged"
               Background="White" BorderBrush="#E0E0E0" BorderThickness="1">
                        <ComboBoxItem Content="Tất cả hoạt động" IsSelected="True"/>
                        <ComboBoxItem Content="Xuất Excel"/>
                        <ComboBoxItem Content="In phiếu"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <!-- Thông báo lọc -->
            <TextBlock Grid.Row="2" x:Name="lblFilterInfo" Margin="20,5,20,0" 
                       Foreground="Blue" FontStyle="Italic" 
                       HorizontalAlignment="Left" VerticalAlignment="Top" 
                       TextWrapping="Wrap" Visibility="Collapsed"/>

            <!-- Danh sách lịch sử bảo trì -->
            <Border Grid.Row="3" BorderThickness="0" Margin="20,10">
                <DataGrid x:Name="dgLichSu" AutoGenerateColumns="False" 
                HeadersVisibility="Column"
                BorderThickness="1" BorderBrush="#E0E0E0"
                Background="White" RowBackground="White"
                AlternatingRowBackground="#F8F8F8" 
                GridLinesVisibility="All"
                CanUserAddRows="False" CanUserDeleteRows="False" 
                IsReadOnly="True"
                ColumnHeaderHeight="50" RowHeight="45" 
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto" 
                ItemsSource="{Binding DanhSachLichSu}">
                    <DataGrid.Resources>
                        <Style TargetType="{x:Type DataGridColumnHeader}">
                            <Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#C0C0C0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="{x:Type DataGridCell}">
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#D4E6F1"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="BorderBrush" Value="#C0C0C0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <StackPanel Margin="10">
                                <TextBlock Text="Chi tiết tài sản:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ChiTietTaiSan}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Ngày thực hiện" Width="150" Binding="{Binding LichSu.NgayThucHien, StringFormat=dd/MM/yyyy HH:mm:ss}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Loại hoạt động" Width="120" Binding="{Binding LichSu.LoaiHoatDong}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="Thông tin tài sản" Width="250">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="5,3">
                                        <TextBlock Text="{Binding LichSu.TenTaiSan}" FontWeight="Medium" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding LichSu.SoSeri, StringFormat=Số sê-ri: {0}}" Foreground="#555" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Người thực hiện" Width="150" Binding="{Binding LichSu.TenNguoiThucHien}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Số lần bảo trì" Width="100" Binding="{Binding SoLanBaoTri}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Ghi chú" Width="*" Binding="{Binding LichSu.GhiChu}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Các nút điều khiển -->
            <Grid Grid.Row="4" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Nút bên trái -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="btnXuatBaoCao" 
                            Content="Xuất báo cáo" 
                            Style="{StaticResource PrimaryButton}"
                            Click="btnXuatBaoCao_Click"/>
                </StackPanel>

                <!-- Nút bên phải -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="btnDong" 
                            Content="Đóng" 
                            Style="{StaticResource PrimaryButton}"
                            Background="#e74c3c"
                            Click="btnDong_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>